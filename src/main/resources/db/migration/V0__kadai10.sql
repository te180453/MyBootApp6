create table `books`(
    `id` INT NOT NULL AUTO_INCREMENT,
    `title` TEXT NOT NULL,
    `writter` TEXT NOT NULL,
    `publisher` TEXT NOT NULL,
    `price` INTEGER NOT NULL,
    PRIMARY KEY (`id`)
)